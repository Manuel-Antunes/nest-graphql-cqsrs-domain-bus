/** Gravada na *classe da subscription*: `{ id }`, o id pelo qual o `SubscriptionBus` acha o handler. */
export const SUBSCRIPTION_METADATA = '__subscription__';
/** Gravada na *classe do handler*: a classe da subscription que ele trata. É o que o explorer procura. */
export const SUBSCRIPTION_HANDLER_METADATA = '__subscriptionHandler__';
