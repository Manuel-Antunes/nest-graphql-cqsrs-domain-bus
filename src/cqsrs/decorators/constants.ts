/** Stored on the *subscription class*: `{ id }`, the id the `SubscriptionBus` looks the handler up by. */
export const SUBSCRIPTION_METADATA = '__subscription__';
/** Stored on the *handler class*: the subscription class it handles. This is what the explorer scans for. */
export const SUBSCRIPTION_HANDLER_METADATA = '__subscriptionHandler__';
